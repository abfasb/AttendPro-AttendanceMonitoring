import React, { useState } from "react";
import ReactQRCode from "react-qr-code";

const Analytics : React.FC = () => {

  return (
    <div>
      <h1>hello this is analytics</h1>
    </div>
  );
};

export default Analytics;
